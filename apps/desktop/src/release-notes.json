{
  "version": "1.1.1",
  "name": "DeepSeek Harness for Win v1.1.1",
  "publishedAt": "2026-08-15",
  "sections": [
    {
      "key": "fixes",
      "titleEn": "Bug Fixes",
      "titleZh": "问题修复",
      "itemsEn": [
        "Fixed in-app updates not starting after restart confirmation: the updater is now launched so Windows PowerShell 5.1 actually executes it (the previous detached launch exited silently, leaving the update stuck at \"starting\" and reporting \"last update incomplete\" on the next launch).",
        "Fixed updater self-termination risk during shell shutdown: the old desktop shell is terminated without recursively killing its process tree, so the running updater can no longer be killed by its own cleanup.",
        "Fixed unexplained incomplete-update states: early updater failures (for example a missing updater module) now record a concrete failed status with the real error message."
      ],
      "itemsZh": [
        "修复应用内确认重启后更新不执行的问题：更新器改为以 Windows PowerShell 5.1 可正常执行的方式启动（此前分离式启动会静默退出，更新一直卡在 \"starting\"，下次启动提示\"上次更新未完成\"）。",
        "修复更新器在关闭旧外壳时可能被误杀的问题：旧桌面外壳改为单进程终止，不再递归击杀其进程树，运行中的更新器不会再被自己的清理逻辑杀死。",
        "修复无法解释的\"更新未完成\"状态：更新器早期失败（如更新模块缺失）现在会写入包含真实错误信息的具体失败状态。"
      ]
    },
    {
      "key": "improvements",
      "titleEn": "Improvements",
      "titleZh": "功能改进",
      "itemsEn": [
        "Console updates (在线更新.bat / update.cmd) reuse a previously downloaded, SHA-256 verified update package, so updating after a failed in-app restart does not re-download.",
        "The post-update launch no longer forces a hidden initial window; the new version's splash and window appear normally."
      ],
      "itemsZh": [
        "控制台更新（在线更新.bat / update.cmd）会复用先前已下载并通过 SHA-256 校验的更新包，应用内重启失败后无需重复下载。",
        "更新完成后不再强制隐藏初始窗口，新版本的启动过渡页与主窗口正常展示。"
      ]
    }
  ],
  "body": "## Bug Fixes / 问题修复\n\n- Fixed in-app updates not starting after restart confirmation: the updater is now launched so Windows PowerShell 5.1 actually executes it. / 修复应用内确认重启后更新不执行的问题：更新器改为以 Windows PowerShell 5.1 可正常执行的方式启动。\n- Fixed updater self-termination risk during shell shutdown: the old desktop shell is terminated without recursively killing its process tree. / 修复更新器在关闭旧外壳时可能被误杀的问题：旧桌面外壳改为单进程终止，不再递归击杀其进程树。\n- Fixed unexplained incomplete-update states: early updater failures now record a concrete failed status with the real error message. / 修复无法解释的\"更新未完成\"状态：更新器早期失败现在会写入包含真实错误信息的具体失败状态。\n\n## Improvements / 功能改进\n\n- Console updates reuse a previously downloaded, SHA-256 verified update package, so updating after a failed in-app restart does not re-download. / 控制台更新会复用先前已下载并通过 SHA-256 校验的更新包，应用内重启失败后无需重复下载。\n- The post-update launch no longer forces a hidden initial window; the new version's splash and window appear normally. / 更新完成后不再强制隐藏初始窗口，新版本的启动过渡页与主窗口正常展示。"
}
